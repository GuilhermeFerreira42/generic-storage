# CONTEXTO PARA IA — GreenForge

## Resumo do Estado Atual
- **Projeto:** GreenForge (Orquestrador Autônomo)
- **Última fase concluída:** 3 — Segurança de Path (SafeResolve)
- **Próxima fase:** 4 — Persistence Layer (SQLite)
- **Status do build:** PASSANDO
- **Status dos testes:** PASSANDO (39/39 testes)
- **Módulos Críticos:** `WorktreeManager`, `SafeResolve`, `AtomicWrite`

## Leitura Obrigatória
1. `docs/CURRENT_STATE.md` — estado atual completo do sistema
2. `src/shared/SafeResolve.ts` — contrato de segurança inviolável

## Leitura no Início de Onda
3. `docs/BACKLOG_FUTURO.md` — roadmap Onda 2

## NUNCA Ler Automaticamente
- `.git/`, `node_modules/`, `dist/`, `build/`
- `docs/archive/*`

## Regra de Contrato
- Use `SafeResolve` em todas as operações de filesystem.
- Use `AtomicWrite` para persistência de estado crítico.
