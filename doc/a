Você é um arquiteto de software especialista em extensões para VS Code, no ecossistema Cline, e em orquestração de agentes de IA estilo Verdant AI.

O usuário tem uma estrutura de pastas no diretório `C:\Users\Usuario\Desktop\xgeneric-storage`. Dentro dela existe:
- `cline-main/`: clone completo do repositório oficial do Cline (https://github.com/cline/cline).
- `doc/`: documentação antiga do projeto GreenForge (para Gemini CLI). Esta pasta contém o **formato, estilo e profundidade** que o usuário considera correto. Você deve usar essa pasta APENAS para aprender o padrão de documentação (como escrever seções, diagramas em texto, tabelas de requisitos, ADRs, playbooks). Não aproveite a arquitetura ou decisões técnicas de lá, pois elas eram para Gemini CLI.
- `doc-old/`: documentação ainda mais antiga. Nela existem **requisitos funcionais e não funcionais** do Verdant AI que foram levantados anteriormente. Você deve consultar essa pasta para extrair todos os requisitos que o GreenForge precisa atender.

Sua tarefa principal é **criar uma nova documentação** para o **GreenForge como um plugin do Cline**. O objetivo final é modificar o código fonte do Cline (pasta `cline-main`) para que ele se torne o GreenForge, um sistema que **replica todas as funcionalidades do Verdant AI**. O Verdant AI é uma ferramenta de orquestração de agentes com worktrees isolados, plan mode estruturado (clarificação + plano revisável), delegação paralela a subagentes especializados, DiffLens para explicar mudanças, sistema de regras (AGENTS.md), e verificação automatizada.

**Você DEVE seguir estas instruções rigorosamente:**

1. **Pesquise na internet** todos os requisitos e características do Verdant AI. Use fontes confiáveis como documentação oficial, artigos técnicos, repositórios GitHub, discussões e benchmarks. Liste as funcionalidades essenciais que diferenciam o Verdant de um agente comum. Anote também como o Verdant lida com paralelismo, subagentes, worktrees, plan mode, DiffLens, regras e verificação.

2. **Consulte a pasta `doc-old`** para extrair requisitos que já foram mapeados anteriormente. Essa documentação antiga pode conter detalhes que a pesquisa na internet não captura. Use esses requisitos como base, mas adapte-os para o novo contexto (Cline).

3. **Consulte a pasta `doc` apenas para aprender o formato de documentação**. Observe como os arquivos são estruturados: a linguagem em prosa, o uso de seções (ex: "1. Introdução", "2. Arquitetura"), os diagramas em texto com caracteres, as tabelas de rastreabilidade, os cenários Gherkin, os ADRs, os playbooks operacionais. Você deve **seguir exatamente esse mesmo estilo** na nova documentação. Não invente um formato novo.

4. **Estude o código-fonte do Cline** na pasta `cline-main` para entender o que pode ser reaproveitado. Você precisa identificar:
   - Se o Cline já possui suporte nativo a worktrees (ex: via Kanban)
   - O sistema de plugins e hooks (`onTaskStart`, `onTaskComplete`, etc.)
   - A existência de subagentes ou execução paralela
   - O mecanismo de regras (`.clinerules`, `AGENTS.md`)
   - Integração com MCP
   - Modelo de segurança (aprovação humana, sandbox)
   - Persistência de estado (checkpoints, workspaceState)
   - Como registrar ferramentas customizadas

   Para cada área, documente no novo design: **o que será reaproveitado** (apenas orquestrado) vs. **o que precisará ser implementado** (porque o Cline não tem). Seja específico, citando arquivos e funções do código do Cline.

5. **Crie uma nova pasta** chamada `doc-greenforge-cline` dentro do diretório raiz (`C:\Users\Usuario\Desktop\xgeneric-storage`). Dentro dela, gere os seguintes arquivos (usando os mesmos nomes da documentação antiga, mas com conteúdo adaptado para o plugin Cline):

   - `GREENFORGE_CLINE_DESIGN.md` – documento principal de design, contendo visão geral, arquitetura do plugin, fluxos de trabalho, decisões arquiteturais (ADRs), mapeamento de requisitos funcionais e não funcionais (todos baseados no Verdant e na doc-old).
   - `01-vision-and-architecture.md` – visão do produto, diferenciais, diagrama de blocos mostrando como o plugin se integra ao Cline e ao VS Code.
   - `02-functional-requirements.md` – lista exaustiva de todos os requisitos funcionais (RF-01 a RF-XX) que o GreenForge deve atender para ser equivalente ao Verdant. Use como base a pesquisa na internet e a doc-old. Exemplos: RF-01: Detecção de intenção (ou comando manual), RF-02: Clarificação com 5-7 perguntas, RF-03: Geração de plano estruturado, RF-04: Isolamento via worktrees, RF-05: Delegação a subagentes, RF-06: Execução paralela com dependências, RF-07: Verificação automática (lint/test), RF-08: DiffLens, RF-09: Sistema de regras (AGENTS.md), RF-10: Aprovação humana, etc.
   - `03-technical-spec-and-data.md` – especificação técnica detalhada: como o plugin se conecta ao SDK do Cline, uso de `workspaceState` para persistência, criação de worktrees via API do git ou comandos shell, registro de hooks e ferramentas, estrutura de dados do orquestrador.
   - `04-operational-playbooks.md` – playbooks para incidentes comuns (worktree órfão, falha de API do LLM, corrupção de estado, conflito de dependências entre subagentes).
   - `05-governance-and-security.md` – modelo de ameaças (T-01 a T-XX), políticas de segurança, herança das salvaguardas do Cline, e as que precisam ser adicionadas.
   - `06-api-and-extensibility.md` – como o plugin expõe comandos no VS Code (ex: `GreenForge: Start Task`, `GreenForge: Approve Plan`), ferramentas customizadas que podem ser chamadas pelo agente, e hooks para extensibilidade futura.
   - `09-hardening-deterministic-contracts.md` – contratos determinísticos para operações críticas: escrita atômica de arquivos, resolução segura de caminhos (anti-path traversal), execução de shell sem risco de injeção, etc.
   - `README.md` – instruções de instalação, ativação do plugin, e exemplos de uso.

6. **O conteúdo deve ser técnico, denso e implementável**. Use prosa fluida, parágrafos contínuos, mas com seções claras (ex: "1. Introdução" em negrito ou separado por linhas em branco). Evite markdown para títulos; use texto puro. Inclua trechos de código TypeScript reais, mostrando como registrar um plugin no Cline, como usar um hook, como criar um worktree, como fazer a clarificação, etc.

7. **Referencie fontes reais** sempre que possível. Exemplos:
   - "Conforme o código em `cline-main/src/extension.ts`, o hook `onTaskStart` existe e é acionado antes de cada tarefa."
   - "A pesquisa na internet (fonte: docs.verdant.ai/architecture) mostra que o Verdant utiliza DiffLens para gerar explicações das mudanças."
   - "Na pasta `doc-old/02-functional-requirements.md` o RF-03 já especificava a necessidade de worktrees isolados."
   Use caminhos relativos dentro do projeto para citar arquivos.

8. **Importante: não delire.** Se você não encontrar uma informação no código do Cline, documente como "não identificado – será necessário implementar" ou "pendente de confirmação". Se a pesquisa na internet não revelar um requisito do Verdant, use a doc-old como fallback. Nunca invente APIs ou comportamentos que não existem.

9. **Após gerar todos os arquivos**, produza um pequeno resumo (máximo 10 linhas) destacando:
   - As principais diferenças entre a documentação antiga (para Gemini CLI) e a nova (para Cline).
   - As três maiores vantagens de usar o Cline como base em vez de construir do zero.
   - A confirmação de que todos os requisitos do Verdant foram mapeados.

Agora execute a tarefa completamente. Leia o formato da pasta `doc`, extraia requisitos da pasta `doc-old`, pesquise na internet sobre o Verdant, analise o código em `cline-main`, e gere a nova documentação em `doc-greenforge-cline`. Comece.